<%= form_with(model: global_area, local: true) do |form| %>
  <% if global_area.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(global_area.errors.count, "error") %> prohibited this global_area from being saved:</h2>

      <ul>
        <% global_area.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :active %>
    <%= form.check_box :active %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
